import React from 'react';

//TODO: Finish setting up next button

export const NextButton = () => {

    const handleNextPage = e =>{

    };

    return (
        <button
        className="next-button"
        type="button"
        onClick={handleNextPage}
        >Next</button>
    );
    
};