import React from 'react';

//TODO: Finish setting up agree button

export const AgreeButton = () => {

    const handleNextPage = e =>{

    };

    return (
        <button
        className="agree-button"
        type="button"
        onClick={handleNextPage}
        >Agree</button>
    );
    
};